export interface ICompanyWeb {
  companyId: string,
  title: string,
  subtitle: string,
  sendLabel: string,
}

export const companyWeb: ICompanyWeb = {
  companyId: 'companyId',
  title: 'Contact',
	subtitle: 'Do you want a special work? Let’s talk.',
  sendLabel: 'Send'
}